<div class="container">
    <header class="header">
        <nav class="navbar">
            <div class="logo">
                <a href="/" class="logo">
                    <img src="/images/logo.png" alt="logo">
                </a>
            </div>
            <div class="links">
                <ul class="nav-menu">
                    <li class="nav-item">
                        <a href="/" class="nav-link">Home</a>
                    </li>
                    <li class="nav-item">
                        <a href="/products" class="nav-link">Products</a>
                    </li>
                    <% if(user){ %>
                        <% if(user.role==='admin' ){ %>
                            <li class="nav-item">
                                <a class="nav-link" href="/admin/orders">Orders</a>
                            </li>
                            <% } else { %>
                                <li class="nav-item">
                                    <a class="nav-link" href="/customer/orders">Orders</a>
                                </li>
                                <% } %>
                                    <li class="nav-item">
                                        <form id="logout" action="/account/logout" method="POST">
                                            <a class="nav-link"
                                                onclick="document.querySelector('#logout').submit()">Logout</a>
                                        </form>
                                    </li>
                                    <% } else { %>
                                        <li class="nav-item">
                                            <a class="nav-link" href="/account/login">Account</a>
                                        </li>
                                        <% } %>
                </ul>
                <div class="hamburger">
                    <span class="bar"></span>
                    <span class="bar"></span>
                    <span class="bar"></span>
                </div>
                <div class="cart">
                    <a id="cart-link" href="/cart" class=<%=session.cart && session.cart.totalQty>0 ? "cart-link":"" %>>
                        <p id="cart-counter">
                            <% if(session.cart && session.cart.totalQty>0){ %>
                                <%= session.cart.totalQty%>
                                    <% } %>
                        </p>
                        <div class="cart-img">
                            <img src="/images/cart.png" alt="">
                        </div>
                    </a>
                </div>
            </div>
        </nav>
    </header>
</div>